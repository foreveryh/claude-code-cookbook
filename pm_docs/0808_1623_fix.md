# Work Log

Summary
- Optimized Bash PreToolUse hooks to reduce latency and avoid unnecessary checks. Added repository guards to PR creation docs to prevent git operations outside a repo.

Work Process
- Updated settings.json.template: split PreToolUse for Bash so that:
  - Bash(git commit*): runs ~/.claude/scripts/check-ai-commit.sh only
  - Bash: runs ~/.claude/scripts/deny-check.sh
- Synced the same changes to ~/.claude/settings.json to take effect immediately.
- Optimized ~/.claude/scripts/deny-check.sh:
  - Added fast path for obviously safe commands
  - Reduced jq invocations to a single parse
  - Cached deny patterns in /tmp and reload only if settings.json changes
- Updated pr-create-smart command docs (en/zh) to include a non-repo guard snippet using `git rev-parse --is-inside-work-tree`.

Results
- Bash pre-flight latency should drop notably for safe commands (e.g., echo, ls, cat), as the hook exits early without pattern scans.
- check-ai-commit now only runs for git commit, avoiding overhead on all other Bash commands.
- Kept ja-space-format.sh but made it conditional on CLAUDE_LANGUAGE=ja, so EN/ZH skip it automatically.
- Added master kill switch: set CLAUDE_DISABLE_BASH_PRE_HOOK=1 to bypass Bash PreToolUse hooks for troubleshooting.
- Documentation now instructs to skip PR steps outside Git repositories, avoiding misguiding auto-init flows.
- Modified files:
  - settings.json.template
  - ~/.claude/settings.json
  - ~/.claude/scripts/deny-check.sh
  - versions/en/commands/pr-create-smart.md
  - versions/zh/commands/pr-create-smart.md

Next Steps
- Measure latency before/after with:
  - `time bash -lc 'echo ok'`
  - `time bash -lc 'ls'`
- Verify safety paths:
  - `bash -lc 'rm -rf /'` should be rejected
  - In a Git repo: `bash -lc "git commit -m '🤖 Generated with ...'"` should be blocked by the hook; normal messages should pass the hook
- If any residual slowness remains, profile per-command timing and consider further narrowing the deny-check fast-path patterns.
- For Japanese installs, consider adding ja-space-format.sh back only in a ja-specific template step if formatting is required.

Notes
- If you add or modify deny patterns in settings.json, the cache is automatically refreshed.
